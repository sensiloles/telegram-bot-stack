# 📝 Инструкция по созданию первого GitHub Issue

Я подготовил всё для автоматического создания issue! Осталось только получить GitHub токен.

## 🚀 Быстрый старт (3 минуты)

### Шаг 1: Создать GitHub токен

1. **Откройте:** https://github.com/settings/tokens
2. **Нажмите:** "Generate new token" → "Generate new token (classic)"
3. **Заполните:**
   - Note: `quit-smoking-bot-cli`
   - Expiration: 30 days (или по вашему выбору)
   - ✅ Выберите scope: **`repo`** (полный доступ к репозиториям)
4. **Нажмите:** "Generate token"
5. **Скопируйте токен** (он показывается только один раз!)

### Шаг 2: Установить токен

Выберите один из способов:

**A. Через .env файл (рекомендуется):**

```bash
# Добавьте в файл .env
echo 'GITHUB_TOKEN=your_token_here' >> .env
```

**B. Через переменную окружения:**

```bash
export GITHUB_TOKEN='your_token_here'
```

### Шаг 3: Создать issue

```bash
# Активировать виртуальное окружение
source venv/bin/activate

# Запустить скрипт создания issue
python3 scripts/create_github_issue.py
```

**Готово!** 🎉

---

## 📋 Альтернатива: Создать issue вручную

Если не хотите использовать токен, можно создать issue вручную:

### Шаг 1: Перейти на GitHub

```
https://github.com/YOUR-USERNAME/quit-smoking-bot/issues/new/choose
```

### Шаг 2: Выбрать шаблон

Выберите **"🎯 Feature Request"**

### Шаг 3: Скопировать содержимое

Откройте файл `.github/FIRST_ISSUE_DRAFT.md` и скопируйте содержимое:

```bash
# Показать содержимое в терминале
cat .github/FIRST_ISSUE_DRAFT.md
```

Или откройте файл в IDE и скопируйте.

### Шаг 4: Заполнить форму

- **Title:** `[Refactor] Phase 0.1: Extract Reusable Components into src/core/`
- **Description:** Скопируйте содержимое из FIRST_ISSUE_DRAFT.md
- **Labels:** Добавьте вручную:
  - `refactor`
  - `component:bot`
  - `priority:high`

### Шаг 5: Submit

Нажмите "Submit new issue"

---

## 🤖 После создания issue

### Попробуйте Cloud Agent команды!

В комментарии к созданному issue добавьте:

```
/breakdown
```

Cloud Agent автоматически разобьёт задачу на подзадачи!

Другие полезные команды:

```
/accept       # Сгенерировать acceptance criteria
/estimate     # Оценить время
/relate       # Найти связанные файлы
```

---

## 🔧 Что делает скрипт?

Скрипт `scripts/create_github_issue.py`:

1. ✅ Читает содержимое из `.github/FIRST_ISSUE_DRAFT.md`
2. ✅ Определяет ваш GitHub репозиторий автоматически
3. ✅ Создаёт issue с правильными лейблами
4. ✅ Показывает ссылку на созданный issue

**Безопасность:**

- Токен хранится локально в `.env` (файл в `.gitignore`)
- Токен НЕ коммитится в git
- Вы можете удалить токен после использования

---

## 💡 Полезные советы

### Проверить наличие токена

```bash
# Проверить в .env
grep GITHUB_TOKEN .env

# Проверить в окружении
echo $GITHUB_TOKEN
```

### Удалить токен после использования

```bash
# Из .env
sed -i '' '/GITHUB_TOKEN/d' .env

# Из окружения
unset GITHUB_TOKEN
```

### Отозвать токен на GitHub

1. Перейдите: https://github.com/settings/tokens
2. Найдите ваш токен
3. Нажмите "Delete" или "Revoke"

---

## 🎯 Что дальше?

После создания первого issue:

1. **Начните работу** над рефакторингом
2. **Используйте Cloud Agent** для автоматизации
3. **Создайте следующие issues** из плана:
   - Phase 0.2: Creating Tests
   - Phase 0.3: Verification
   - Phase 1.1: New Repository Setup

---

## 📞 Нужна помощь?

- 📖 [Cloud Agent Guide](.github/CLOUD_AGENT_GUIDE.md)
- 🤖 [Cloud Agent README](.github/CLOUD_AGENT_README.md)
- 📝 [Issue Draft](.github/FIRST_ISSUE_DRAFT.md)

**Удачи с первым issue!** 🚀
