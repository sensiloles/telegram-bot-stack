"""Unit tests for telegram-bot-stack.

Unit tests are fast, isolated tests that test individual functions, methods,
and classes without external dependencies (databases, networks, Docker, etc.).

Run unit tests:
    pytest tests/unit/
    make test-unit
"""
